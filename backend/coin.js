// 10 seconds tk bet lagao
// 10 second tk coin ghumega
// 10 second tak coin rukega aur next round ka intezaar karega

const toss = () => {
 /* console.log("Points lagao moment has started!");
  const randomVal = Math.random();
  const faceCoin = randomVal < 0.5 ? 'H' : 'T';
  console.log(`${faceCoin}`);
  setTimeout(() => {
    console.log("Ab coin ghum raha hai!");
    setTimeout(() => {
      console.log("Ab result dikh raha hai aur next round ka wait kar rahe hai!");
      console.log(`Result is ${faceCoin}`);
    }, 10000);
  }, 10000);*/
};

export default toss;
